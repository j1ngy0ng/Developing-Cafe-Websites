function toggleInfo(container) {
  const infoOverlay = container.querySelector('.info-overlay');
  infoOverlay.style.display = infoOverlay.style.display === 'block' ? 'none' : 'block';
}

function toggleInfo(infoOverlay) {
}

